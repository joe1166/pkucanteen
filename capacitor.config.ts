import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.pkucanteen',
  appName: 'pku食堂',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
